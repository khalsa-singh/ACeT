# Table S15 viscosity rows

The viscosity rows report the complete defined candidate set screened on the 52 development antibodies. Definitions are in `../DevelopmentOnlyFeatureSelection/viscosity/reference/candidate_definition.csv`; the numeric candidate data and row mapping are in that endpoint's `00_source/` directory. The four selected assays and recorded model performance remain linked to the corresponding numeric model-input files.

Use `python acet.py viscosity-screen` from the repository root to regenerate the ranks and correlations.
